<Window x:Class="Discord365.UI.MainClientWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Discord365.UI"
        xmlns:c="clr-namespace:Discord365.UI.User.CustomContent"
        xmlns:user="clr-namespace:Discord365.UI.User"
        mc:Ignorable="d"
        Title="Discord365" Height="450" Width="800" WindowStartupLocation="CenterScreen" WindowState="Maximized" Closing="Window_Closing">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF289AB4" Offset="0"/>
            <GradientStop Color="#FF691B6C" Offset="1"/>
            <GradientStop Color="#FF443A83" Offset="0.466"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid x:Name="DiscordContent" Visibility="Visible" Background="#26000000">
            <Grid.Effect>
                <BlurEffect x:Name="ContentBlur" Radius="0"/>
            </Grid.Effect>
            <user:DMPanel x:Name="DMPanel" Width="69" VerticalAlignment="Stretch" HorizontalAlignment="Left"></user:DMPanel>
            <user:SideBar x:Name="Sidebar" Width="240" VerticalAlignment="Stretch" HorizontalAlignment="Left" Margin="69,0,0,0"></user:SideBar>
            <user:ContentBasic x:Name="ContentBasic" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="309,0,0,0"></user:ContentBasic>
        </Grid>

        <Grid x:Name="DiscordCustom" Visibility="Visible" Background="#AD000000">
            <c:CustomCtrlWnd x:Name="CustomCtrl"></c:CustomCtrlWnd>
        </Grid>

        <Grid x:Name="DiscordStatus" Background="#AD000000" Visibility="Hidden">
            <TextBlock x:Name="tbDiscordStatus" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="18" Text="I Like This Text!"></TextBlock>
        </Grid>
    </Grid>
</Window>
